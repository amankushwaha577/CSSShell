// CSS Priority Orders :- 
__________________________________________________________________
// +--------------------------------+-----------------------------+
// | CSS Priority Order             | Description                 |
// +--------------------------------+-----------------------------+
// | 1. Inline styles (highest)     | style="color: red;"         |  Highest : Specificity: (1, 0, 0, 0)
// | 2. Internal styles             | Written in <style> tag      |  Specificity depends on the type of selectors (IDs, classes, elements).
// | 3. External styles             | Linked via <link> tag       |  Specificity depends on the type of selectors (IDs, classes, elements).
// | 4. Browser default styles      | Lowest priority             |  Specificity depends on the type of selectors (IDs, classes, elements).
// +--------------------------------+-----------------------------+

__________________________________________________________________
// First Priority of CSS Is taken than specificity